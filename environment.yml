name: saas_bi
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      # Orchestration (local testing of DAG code — runs inside Docker in prod)
      - apache-airflow==2.9.3
      # Transformation
      - dbt-core==1.8.4
      - dbt-postgres==1.8.2
      # API layer
      - fastapi==0.115.0
      - uvicorn[standard]==0.30.6
      - sqlalchemy==2.0.34
      - asyncpg==0.29.0
      - psycopg2-binary==2.9.9
      - pydantic==2.9.2
      - pydantic-settings==2.5.2
      # Frontend
      - streamlit==1.38.0
      - plotly==5.24.0
      # Data engineering
      - pandas==2.2.2
      - numpy==1.26.4
      - beautifulsoup4==4.12.3
      - lxml==5.3.0
      - requests==2.32.3
      - httpx==0.27.2
      # Utilities
      - python-dotenv==1.0.1
      - tenacity==9.0.0
      # Tests / dev
      - pytest==8.3.3
      - black==24.8.0
      - ruff==0.6.4
